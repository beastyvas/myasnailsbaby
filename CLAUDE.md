# MyasNailsBaby — Claude Instructions

## Git Workflow

- Always work directly on `main`. Never create feature branches.
- Commit changes to `main` and push to `origin main`.
- Do not create pull requests unless explicitly asked.
